// Buffer

// O que é? 
//
// o Buffer é uma representação de um espaço na memória do computador, usado especificamente para transitar dados em alta velocidade.

// Os dados armazenados no buffer é usado para ser usados de maneiras rapidas.

// O buffer é mais performatico, pois é mais perfomatico ler uma informação de uma forma binária do que de outra forma.

//exemplo

const buf = Buffer.from("OK")

console.log(buf.toJSON())